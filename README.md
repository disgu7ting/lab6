# Лабораторная работа №6  
## Асинхронный JavaScript: API и хранилище



## 📌 Вопросы по отчёту

### **1. Что делает fetch()?**
`fetch()` отправляет HTTP-запрос к серверу и возвращает Promise.  
Когда сервер ответит — Promise переходит в состояние *fulfilled* и возвращает объект Response.

### **2. Зачем нужны async/await?**
Они позволяют писать асинхронный код так, будто он синхронный.  
`await` останавливает выполнение функции, пока Promise не выполнится.

### **3. Как работает localStorage?**
Это встроенное хранилище браузера:  
- хранит данные в формате *ключ-значение*  
- данные доступны даже после перезагрузки страницы  
- хранит **только строки**, объекты нужно сериализовать через `JSON.stringify()`  

### **4. Где помог ИИ?**
- Генерация структуры блока отзывов  
- Код получения цитат через fetch + async/await  
- Код загрузки изображений   
- Вручную пришлось адаптировать стили, подключить скрипты и интегрировать в верстку.
